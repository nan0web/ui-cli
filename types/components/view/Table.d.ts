/**
 * Table Component.
 * Can be static (display only) or interactive (filter/select).
 */
export function Table(props: any): {
    $$typeof: symbol;
    type: string;
    props: any;
    execute: () => any;
};
